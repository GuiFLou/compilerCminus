/* Fatorial recursivo, com local `acc` em cada ativacao */

int fact(int n)
{
    int acc;
    if (n < 2) return 1;
    acc = fact(n - 1);
    return n * acc;
}

void main(void)
{
    int n;
    int r;
    n = input();
    r = fact(n);
    output(r);
}
