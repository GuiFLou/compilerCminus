/* Calcula a area de um triangulo de base 10 e altura 6. */

void main(void)
{
    int base;
    int altura;
    int area;
    base = 10;
    altura = 6;
    area = base * altura / 2;
    output(area);
}
