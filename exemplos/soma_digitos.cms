/* Soma os algarismos de um numero. */

void main(void)
{
    int numero;
    int digito;
    int soma;
    numero = input();
    soma = 0;
    while (numero > 0) {
        digito = numero - numero / 10 * 10;
        soma = soma + digito;
        numero = numero / 10;
    }
    output(soma);
}
