/* Calcula a area de um triangulo a partir da base e da altura. */

void main(void)
{
    int base;
    int altura;
    int area;
    base = input();
    altura = input();
    area = base * altura / 2;
    output(area);
}
